[package]
name = "ahci_driver"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
kernel_api = { path = "../../interfaces/kernel_api" }

# HAL for MMIO and port I/O wrappers
hal = { path = "../../hal" }
spin = "0.9"
