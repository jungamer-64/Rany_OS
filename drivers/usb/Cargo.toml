[package]
name = "usb_driver"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
# HAL for MMIO and port I/O wrappers
hal = { path = "../../hal" }
spin = "0.9.8"
kernel_api = { path = "../../interfaces/kernel_api" }
