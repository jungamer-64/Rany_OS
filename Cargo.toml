[workspace]
resolver = "2"
members = [
    # Test orchestration (2-layer: crate-local pure + full-boot QEMU)
    "qemu-tests",
    "tools/qemu_runner",
    # Core
    "kernel",
    "hal",
    "interfaces/kernel_api",
    # Libs
    "libs/app_sdk",
    "libs/security",
    "libs/boot_config",
    "libs/graphic_types",
    "filesystems/vfs",
    "libs/sync",
    # Filesystems
    "filesystems/fat32",
    "filesystems/nvme_ns",
    # Drivers
    "drivers/pci",
    "drivers/ahci",
    "drivers/usb",
    "drivers/nvme",
    "drivers/serial",
    "drivers/gpu",
    "drivers/hid",
    "drivers/hda",
    "drivers/virtio",
    "drivers/acpi",
    "drivers/rtc",
    "drivers/ide",
    "drivers/time",
    "drivers/example_abi",
    "drivers/mlx5",
    "tools/driver_cell_probe",
    # Apps
    "apps",
    "libs/boot_proto",
    "bootloader",
    # dev tool: symbol lookup helper (not part of standard critical tools)
    # "tools/symlookup",
    "tools/cap_harness",
    # NOTE: `tools/framebuffer_bench`, `tools/signer`, and `tools/symlookup` are intentionally not workspace members
    # to avoid workspace-wide `build-std` / duplicate-lang-item (E0152) issues.
    # They are runnable via their manifest paths.
]
exclude = ["tools/framebuffer_bench", "tools/signer", "tools/symlookup"]
default-members = [
    "kernel",
    "libs/boot_config",
    "libs/security",
    "interfaces/kernel_api",
    "libs/sync",
    "libs/graphic_types",
    "filesystems/vfs",
    "filesystems/fat32",
    "filesystems/nvme_ns",
    "drivers/pci",
    "drivers/ahci",
    "drivers/usb",
    "drivers/nvme",
    "drivers/hid",
    "drivers/hda",
    "drivers/virtio",
    "drivers/acpi",
    "drivers/time",
    "drivers/mlx5",
    "apps",
    "tools/cap_harness",
]

[workspace.package]
edition = "2024"
authors = ["ExoRust Team"]
license = "MIT"
repository = "https://github.com/Start-Rany-OS/Rany_OS"
readme = "README.md"
keywords = ["os", "kernel", "rust"]
categories = ["os"]

# ============================================================================
# ブートイメージ設定（Workspace全体）
# ============================================================================

[workspace.metadata.bootimage]
build-command = ["build"]
run-command = [
    "qemu-system-x86_64",
    "-drive",
    "format=raw,file={}",
    "-serial",
    "stdio",
    "-device",
    "isa-debug-exit,iobase=0xf4,iosize=0x04",
    "-display",
    "none",
]

test-args = [
    "-device",
    "isa-debug-exit,iobase=0xf4,iosize=0x04",
    "-serial",
    "stdio",
    "-display",
    "none",
]
test-success-exit-code = 33
test-timeout = 300

# ============================================================================
# ビルドプロファイル
# ============================================================================

[profile.dev]
panic = "abort"
opt-level = 0
debug = true
# Use split-debuginfo to keep DWARF and other debug sections out-of-band for dev builds.
# This avoids embedding large .debug* sections in the kernel ELF which can cause
# loadable segment file-offset overlaps when linking with lld.
split-debuginfo = "unpacked"


#[profile.dev.package."*"]
#opt-level = 3
#debug = false
#NOTE: package-specific dev overrides are intentionally disabled because
#they caused build profile inconsistencies between dev and test which led
#to duplicate compilation of core with differing panic strategies.

[profile.release]
panic = "abort"
lto = true
incremental = false
codegen-units = 1
opt-level = 3
# Disable debug info and strip symbols for release kernel images to avoid
# embedding large debug/symbol sections that can overlap loadable ranges.
debug = false
strip = "symbols"
