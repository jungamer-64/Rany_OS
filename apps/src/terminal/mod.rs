// apps/src/terminal/mod.rs - Terminal Application Stub
//!
//! Terminal emulator (stub).

#![allow(dead_code)]

/// Terminal stub
pub struct Terminal;

impl Terminal {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Terminal {
    fn default() -> Self {
        Self::new()
    }
}
