[package]
name = "security"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Security Primitives for RanyOS"

[lints]
workspace = true

[dependencies]
spin = "0.10"
log = "0.4"

[features]
# Default to no-std; enable std explicitly for host tests via `--features std`
default = []
std = []
