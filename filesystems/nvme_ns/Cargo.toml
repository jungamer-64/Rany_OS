[package]
name = "nvme_ns"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
keywords.workspace = true
description = "NVMe Namespace-based filesystem for RanyOS — direct block access without VFS overhead"
categories.workspace = true

[lib]
name = "nvme_ns"
path = "src/lib.rs"

[dependencies]
vfs = { path = "../vfs" }
exorust_sync = { path = "../../libs/sync" }
log = "0.4"
hashbrown = "0.16"

[features]
debug-trace = []
